class ChatWidgetMessage {
  final String text;
  final bool isMe;
  final DateTime timestamp;

  ChatWidgetMessage({
    required this.text,
    required this.isMe,
    required this.timestamp,
  });
}
